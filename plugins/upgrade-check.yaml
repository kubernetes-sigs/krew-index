apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: upgrade-check
spec:
  version: v0.1.3
  homepage: https://github.com/runtimez-com/kube-upgrade-check
  shortDescription: Find what breaks before you upgrade Kubernetes
  description: |
    Reports what an upgrade to a target Kubernetes version would break in the
    cluster you are pointed at: objects still written at an API version the
    target removes, control-plane and kubelet settings it refuses to start
    with, in-tree volume plugins that stop mounting, node runtime and version
    skew, and whether add-ons such as Karpenter, ingress-nginx, cert-manager,
    Argo CD, CoreDNS and Istio support the version you are moving to.

    It reads the cluster and writes nothing. No account and no agent, and no
    data leaves the machine.

    Removed APIs are found through managed fields, so an object is caught
    whatever applied it, including server-side apply, operators and GitOps
    controllers that leave no last-applied annotation behind.

    Every check that could not run is printed with the reason and a command to
    run yourself, so a clean report cannot be one where half the checks were
    silently skipped.

    Examples:
      kubectl upgrade-check --target 1.34
      kubectl upgrade-check --target 1.34 -o json
      kubectl upgrade-check --fail-on high --strict
  caveats: |
    Some checks need permissions the built-in view role does not grant:

      * nodes/proxy, to read each node's live kubelet configuration
      * the /metrics endpoint, for one of the removed-API evidence sources

    Without them the affected checks are reported as unavailable rather than
    skipped silently. A read-only ClusterRole covering everything is in the
    repository under rbac/.
  platforms:
    - selector:
        matchLabels:
          os: darwin
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.3/kube-upgrade-check_0.1.3_darwin_amd64.tar.gz
      sha256: 887c48d0063aa5099a40e2bfbf427ff3e86b75d78c4ef76889d43659cb5df1b9
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: darwin
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.3/kube-upgrade-check_0.1.3_darwin_arm64.tar.gz
      sha256: 8e2b73a67032f453cc6f4f3c71f8013240f707f9ec7718a8470558910925a090
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: linux
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.3/kube-upgrade-check_0.1.3_linux_amd64.tar.gz
      sha256: d790dac352e92df9e0334e9121169e82cfa51c97e64975b22e50110cd21d1a43
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: linux
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.3/kube-upgrade-check_0.1.3_linux_arm64.tar.gz
      sha256: de236ba5fe34ca58263eafec3cb0d1604c1313929075e6f39c5411544bd0a796
      bin: kube-upgrade-check
      files:
        - from: "kube-upgrade-check"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: windows
          arch: amd64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.3/kube-upgrade-check_0.1.3_windows_amd64.zip
      sha256: 50a7541e40af50735b344bfc5f14c1aa5cea0a406e546bb24d482ea9b8d434af
      bin: kube-upgrade-check.exe
      files:
        - from: "kube-upgrade-check.exe"
          to: "."
        - from: LICENSE
          to: "."
    - selector:
        matchLabels:
          os: windows
          arch: arm64
      uri: https://github.com/runtimez-com/kube-upgrade-check/releases/download/v0.1.3/kube-upgrade-check_0.1.3_windows_arm64.zip
      sha256: a34273a94cb83a7403eec1463cd549cd26bd1f4b4080abae13dbe15f880d0a72
      bin: kube-upgrade-check.exe
      files:
        - from: "kube-upgrade-check.exe"
          to: "."
        - from: LICENSE
          to: "."
