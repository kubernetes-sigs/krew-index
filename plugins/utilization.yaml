apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: utilization
spec:
  version: "v1.0.2"
  platforms:
  - selector:
      matchExpressions:
      - {key: os, operator: In, values: [darwin, linux]}
    uri: https://github.com/etopeter/kubectl-utilization/releases/download/v1.0.3/kubectl-utilization-v1.0.3.tar.gz
    sha256: "288e05b9015aeee918703633c3f66e840a69c5c3106ee19ff21f9593f202fb89"
    bin: "kubectl-utilization"
    files:
    - from: "*"
      to: "."
  shortDescription: Shows cluster cpu and memory utilization
  caveats: |
    This plugin needs the following programs:
    * awk
    * bc
  description: |
    This plugin shows cluster resource utilization based on cpu and memory. It collects pod requests and node available resources to calculate metrics.
