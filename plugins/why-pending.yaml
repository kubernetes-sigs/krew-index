apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: why-pending
spec:
  version: v0.3.1
  homepage: https://github.com/SaiRohithGuntupally/kubectl-why-pending
  shortDescription: Explain why a pod is stuck Pending.
  description: |
    why-pending re-runs the scheduler's filtering logic locally and explains, in
    plain English, why a pod cannot be scheduled — and what to change.

    It calls out the causes bare-metal / on-prem clusters hit that cloud clusters
    hide behind the autoscaler: resource fragmentation (capacity exists, but not
    on any single node), control-plane taints eating a small cluster, GPU /
    extended resources (no node provides it, or none has enough free), topology
    spread skew, anti-affinity running out of hosts, a pod larger than any node,
    nodeSelector / nodeAffinity mismatches, cordoned / NotReady nodes, and unbound
    PersistentVolumeClaims. When no static blocker is found it points at the
    dynamic causes it doesn't model and shows the raw scheduler event.

    Usage:
      kubectl why-pending                 # every Pending pod in the namespace
      kubectl why-pending my-pod -n data  # a specific pod
      kubectl why-pending -A              # all namespaces
  caveats: |
    Best-effort static analysis. It models the common scheduler predicates but not
    priority/preemption, extended/custom resources (GPUs, hugepages), or topology
    spread minDomains/nodeAffinityPolicy. It reads cluster state (nodes, pods,
    events, PVCs) and makes no changes.
  platforms:
  - selector:
      matchLabels:
        os: darwin
        arch: arm64
    uri: https://github.com/SaiRohithGuntupally/kubectl-why-pending/releases/download/v0.3.1/kubectl-why-pending_darwin_arm64.tar.gz
    sha256: 2e35e284c6a126c5cbba1e8b87f816cfe3faa5f88c9d98e2c0da3b57ccbd799c
    bin: kubectl-why_pending
  - selector:
      matchLabels:
        os: darwin
        arch: amd64
    uri: https://github.com/SaiRohithGuntupally/kubectl-why-pending/releases/download/v0.3.1/kubectl-why-pending_darwin_amd64.tar.gz
    sha256: ec687feb236519cb0e7a3eeabed6ddac8101eeac6c02a50c7a7b9690d1a296de
    bin: kubectl-why_pending
  - selector:
      matchLabels:
        os: linux
        arch: amd64
    uri: https://github.com/SaiRohithGuntupally/kubectl-why-pending/releases/download/v0.3.1/kubectl-why-pending_linux_amd64.tar.gz
    sha256: ac5b66c6d18bae63d6bdc29d404f5962258d4bde4d58a6d7e8e21c8543aa70ec
    bin: kubectl-why_pending
  - selector:
      matchLabels:
        os: linux
        arch: arm64
    uri: https://github.com/SaiRohithGuntupally/kubectl-why-pending/releases/download/v0.3.1/kubectl-why-pending_linux_arm64.tar.gz
    sha256: 8f2d7619eaee6cb42b869a1cb5b862fe06d372e2b7526ba60172128337afdb0a
    bin: kubectl-why_pending
