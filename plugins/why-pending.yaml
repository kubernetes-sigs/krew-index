apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: why-pending
spec:
  version: v0.4.2
  homepage: https://github.com/SaiRohithGuntupally/kubectl-why-pending
  shortDescription: Explain why a pod is stuck Pending.
  description: |
    why-pending re-runs the scheduler's filtering logic locally and explains, in
    plain English, why a pod cannot be scheduled — and what to change.

    It calls out the causes bare-metal / on-prem clusters hit that cloud clusters
    hide behind the autoscaler: resource fragmentation (capacity exists, but not
    on any single node), control-plane taints eating a small cluster, GPU /
    extended resources (no node provides it, or none has enough free), topology
    spread skew, anti-affinity running out of hosts, a pod larger than any node,
    nodeSelector / nodeAffinity mismatches, cordoned / NotReady nodes, and unbound
    PersistentVolumeClaims. When no static blocker is found it points at the
    dynamic causes it doesn't model and shows the raw scheduler event.

    Usage:
      kubectl why-pending                 # every Pending pod in the namespace
      kubectl why-pending my-pod -n data  # a specific pod
      kubectl why-pending -A              # all namespaces
  caveats: |
    Best-effort static analysis. It models the common scheduler predicates —
    including extended resources (GPUs, hugepages, device-plugin resources) and
    topology spread — but not priority/preemption or the topology-spread
    minDomains/nodeAffinityPolicy refinements. It reads cluster state (nodes, pods,
    events, PVCs) and makes no changes.
  platforms:
  - selector:
      matchLabels:
        os: darwin
        arch: arm64
    uri: https://github.com/SaiRohithGuntupally/kubectl-why-pending/releases/download/v0.4.2/kubectl-why-pending_darwin_arm64.tar.gz
    sha256: a9c365ff030ffab00b3245b43eb9db72aa6750811c51b62066229892beaa3448
    bin: kubectl-why_pending
  - selector:
      matchLabels:
        os: darwin
        arch: amd64
    uri: https://github.com/SaiRohithGuntupally/kubectl-why-pending/releases/download/v0.4.2/kubectl-why-pending_darwin_amd64.tar.gz
    sha256: 2a2e3c9951f045da6a1ccb410fdf3d1a0689a7c6430280b241676cd1fecbe2ff
    bin: kubectl-why_pending
  - selector:
      matchLabels:
        os: linux
        arch: amd64
    uri: https://github.com/SaiRohithGuntupally/kubectl-why-pending/releases/download/v0.4.2/kubectl-why-pending_linux_amd64.tar.gz
    sha256: 352e2472098958e4facc48cf3769b6d84f8b4e53575ff41b332c0ed1a7928a25
    bin: kubectl-why_pending
  - selector:
      matchLabels:
        os: linux
        arch: arm64
    uri: https://github.com/SaiRohithGuntupally/kubectl-why-pending/releases/download/v0.4.2/kubectl-why-pending_linux_arm64.tar.gz
    sha256: 0bd2e6e90eff3e1baea879a7dc68651ded07c787fe53028dc7be30fbd61ea38c
    bin: kubectl-why_pending
